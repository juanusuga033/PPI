import { useEffect, useState } from 'react';import { Link } from 'react-router-dom';import { supabase } from '../lib/supabaseClient'
// Solicita el enlace de recuperación o actualiza la contraseña durante la recuperación.
export default function PasswordReset(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[recovery,setRecovery]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);useEffect(()=>{const {data:{subscription}}=supabase.auth.onAuthStateChange(event=>{if(event==='PASSWORD_RECOVERY')setRecovery(true)});return()=>subscription.unsubscribe()},[]);async function submit(e){e.preventDefault();setLoading(true);setError('');setMessage('');const result=recovery?await supabase.auth.updateUser({password}):await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/recuperar-password`});setLoading(false);if(result.error)return setError(result.error.message);setMessage(recovery?'Tu contraseña fue actualizada correctamente.':'Revisa tu correo: te enviamos un enlace seguro.');if(recovery)setPassword('')}return <div className="auth-shell"><aside className="auth-aside"><h1>Recupera el acceso a tu comunidad.</h1></aside><main className="auth-panel"><section className="auth-form"><span className="eyebrow">{recovery?'Nueva contraseña':'Recuperar contraseña'}</span><h2>{recovery?'Crea una nueva contraseña':'Te ayudamos a volver'}</h2><p style={{textAlign:'left'}}>{recovery?'Elige una contraseña nueva para tu cuenta.':'Escribe tu correo y te enviaremos las instrucciones.'}</p><form onSubmit={submit}>{recovery?<label>Nueva contraseña<input type="password" minLength="6" value={password} onChange={e=>setPassword(e.target.value)} required/></label>:<label>Correo electrónico<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>}{error&&<div className="form-error">{error}</div>}{message&&<div className="form-success">{message}</div>}<button className="button primary" disabled={loading}>{loading?'Procesando…':recovery?'Cambiar contraseña':'Enviar enlace'}</button></form><p><Link to="/login">Volver a iniciar sesión</Link></p></section></main></div>}
