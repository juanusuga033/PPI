import { useCallback, useEffect, useState } from 'react'
import { useSearchParams } from 'react-router-dom'
import { supabase } from '../lib/supabaseClient'
import ProductCard from '../components/ProductCard'
export default function Compra() { const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[search,setSearch]=useState(''),[size,setSize]=useState(''),[order,setOrder]=useState('recent'); const [params]=useSearchParams(); const category=params.get('categoria') || ''
 const load=useCallback(async()=>{let q=supabase.from('publicaciones').select('*').eq('tipo','venta');if(search)q=q.or(`titulo.ilike.%${search}%,descripcion.ilike.%${search}%`);if(size)q=q.eq('talla',size);q=q.order(order==='price-low'||order==='price-high'?'precio':'created_at',{ascending:order==='price-low'});const {data}=await q;setItems(data||[]);setLoading(false)},[search,size,order]);/* eslint-disable react-hooks/set-state-in-effect */useEffect(()=>{load()},[load]);/* eslint-enable react-hooks/set-state-in-effect */ return <div className="catalog-page"><header className="catalog-hero"><div><span className="eyebrow">Marketplace escolar</span><h1>Compra con propósito</h1></div><p>Encuentra uniformes en excelente estado, cerca de tu comunidad y a precios justos.</p></header><div className="filters"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar camisa, sudadera, colegio…"/><select value={size} onChange={e=>setSize(e.target.value)}><option value="">Todas las tallas</option>{['XS','S','M','L','XL','6','8','10','12','14'].map(x=><option key={x}>{x}</option>)}</select><select defaultValue={category}><option>{category || 'Todas las categorías'}</option><option>Uniformes</option><option>Camisas</option><option>Pantalones</option><option>Sudaderas</option><option>Zapatos</option></select><select value={order} onChange={e=>setOrder(e.target.value)}><option value="recent">Más recientes</option><option value="price-low">Menor precio</option><option value="price-high">Mayor precio</option></select></div>{loading?<div className="page-state">Cargando publicaciones…</div>:items.length?<div className="product-grid">{items.map((x,i)=><ProductCard product={x} index={i} key={x.id}/>)}</div>:<div className="empty-state">No encontramos publicaciones que coincidan con tu búsqueda.</div>}</div> }
