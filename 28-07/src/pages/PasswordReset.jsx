import { useState } from 'react';import { Link } from 'react-router-dom';import { supabase } from '../lib/supabaseClient'
export default function PasswordReset(){const [email,setEmail]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setError('');const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/login`});setLoading(false);if(error)setError(error.message);else setMessage('Revisa tu correo: te enviamos un enlace seguro para restablecer tu contraseña.')}return <div className="auth-shell"><aside className="auth-aside"><h1>Recupera el acceso a tu comunidad.</h1></aside><main className="auth-panel"><section className="auth-form"><span className="eyebrow">Recuperar contraseña</span><h2>Te ayudamos a volver</h2><p style={{textAlign:'left'}}>Escribe tu correo y te enviaremos las instrucciones.</p><form onSubmit={submit}><label>Correo electrónico<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}{message&&<div className="form-success">{message}</div>}<button className="button primary" disabled={loading}>{loading?'Enviando…':'Enviar enlace'}</button></form><p><Link to="/login">Volver a iniciar sesión</Link></p></section></main></div>}
