import { Link } from 'react-router-dom'
import { getCatalogImage } from '../lib/catalogImages'
export default function ProductCard({ product, index = 0 }) { const donation = product.tipo === 'donacion'; return <article className="product-card"><Link to={`/producto/${product.id}`} className="product-image"><img src={product.imagen_url || getCatalogImage(index)} alt={product.titulo || 'Uniforme disponible'} /><span className={`type-badge ${donation ? 'donation' : ''}`}>{donation ? 'Donación' : 'Venta'}</span></Link><div className="product-body"><div className="product-top"><span className="product-category">{product.categoria || 'Uniformes'}</span><span className="condition-badge">{product.condicion || 'Buen estado'}</span></div><h3>{product.titulo || 'Uniforme disponible'}</h3><p className="product-meta">Talla {product.talla || 'Por confirmar'} · Comunidad educativa</p><div className="product-bottom"><strong>{donation ? 'Gratis' : `$${Number(product.precio || 0).toLocaleString('es-CO')}`}</strong><Link to={`/producto/${product.id}`} className="text-link">Ver producto →</Link></div></div></article> }
