import { useState } from 'react'; import { Link,useNavigate } from 'react-router-dom'; import { supabase } from '../lib/supabaseClient'
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false);const navigate=useNavigate();async function submit(e){e.preventDefault();setLoading(true);setError('');const {error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error)return setError(error.message);navigate('/')}return <div className="auth-shell"><aside className="auth-aside"><span className="eyebrow">DonayViste · comunidad circular</span><h1>Lo que cuidas, vuelve a servir.</h1><p>Ingresa para comprar, publicar y compartir uniformes que todavía tienen mucho por dar.</p></aside><main className="auth-panel"><section className="auth-form"><span className="eyebrow">Bienvenida de vuelta</span><h2>Inicia sesión</h2><p style={{textAlign:'left'}}>Continúa en tu comunidad DonayViste.</p><form onSubmit={submit}><label>Correo electrónico<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Contraseña<div style={{display:'flex',gap:'.4rem'}}><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)}>{show?'Ocultar':'Ver'}</button></div></label>{error&&<div className="form-error">{error}</div>}<button className="button primary" disabled={loading}>{loading?'Ingresando…':'Iniciar sesión'}</button></form><Link className="auth-secondary-btn" to="/recuperar-password">¿Olvidaste tu contraseña?</Link><p>¿Aún no tienes cuenta? <Link to="/register">Créala aquí</Link></p></section></main></div>}
