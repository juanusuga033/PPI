import { useCallback,useEffect,useState } from 'react'; import { supabase } from '../lib/supabaseClient'; import ProductCard from '../components/ProductCard'
export default function Donaciones(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[search,setSearch]=useState('');const load=useCallback(async()=>{let q=supabase.from('publicaciones').select('*').eq('tipo','donacion').order('created_at',{ascending:false});if(search)q=q.or(`titulo.ilike.%${search}%,descripcion.ilike.%${search}%`);const {data}=await q;setItems(data||[]);setLoading(false)},[search]);/* eslint-disable react-hooks/set-state-in-effect */useEffect(()=>{load()},[load]);/* eslint-enable react-hooks/set-state-in-effect */return <div className="catalog-page"><header className="catalog-hero"><div><span className="eyebrow">Compartir transforma</span><h1>Donaciones</h1></div><p>Prendas listas para empezar una nueva etapa, sin costo para quien las necesite.</p></header><div className="filters"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar una donación…"/><select><option>Todas las tallas</option></select><select><option>Todos los estados</option></select></div>{loading?<div className="page-state">Cargando donaciones…</div>:items.length?<div className="product-grid">{items.map((x,i)=><ProductCard product={x} index={i} key={x.id}/>)}</div>:<div className="empty-state">No encontramos donaciones disponibles por ahora.</div>}</div>}
